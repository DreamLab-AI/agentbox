# Oracle Cloud Infrastructure (OCI) Configuration Template
# Copy this file to .env and fill in your values
# WARNING: Never commit the .env file to version control

# =============================================================================
# REQUIRED: Oracle Cloud Credentials
# =============================================================================

# User OCID - Get from Oracle Console: User Profile → User Settings
# Format: ocid1.user.oc1..aaaaaaaaXXXXXX
OCI_USER_OCID=

# Tenancy OCID - Get from Oracle Console: Administration → Tenancy Details
# Format: ocid1.tenancy.oc1..aaaaaaaaXXXXXX
OCI_TENANCY_OCID=

# Region - Your Oracle Cloud region
# Examples: us-ashburn-1, us-phoenix-1, ap-sydney-1, eu-frankfurt-1
OCI_REGION=

# Fingerprint - Shown when you add your API key in Oracle Console
# Format: xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx
OCI_FINGERPRINT=

# =============================================================================
# OPTIONAL: Additional Configuration
# =============================================================================

# Compartment OCID - Use tenancy OCID for root compartment, or specific compartment
# Format: ocid1.compartment.oc1..aaaaaaaaXXXXXX (or use tenancy OCID)
OCI_COMPARTMENT_OCID=

# API Key File Path - Path to your private key
# Default: ~/.oci/oci_api_key.pem
OCI_KEY_FILE=/home/devuser/.oci/oci_api_key.pem

# API Private Key (base64 encoded) - For containerized deployments
# Generate with: base64 -w0 ~/.oci/oci_api_key.pem
OCI_PRIVATE_KEY_BASE64=

# =============================================================================
# DEPLOYMENT: ARM Free Tier Instance Configuration
# =============================================================================

# Availability Domain - Get from: oci iam availability-domain list
# Example: Uocm:PHX-AD-1
OCI_AVAILABILITY_DOMAIN=

# Subnet OCID - Get from: oci network subnet list
# Format: ocid1.subnet.oc1..aaaaaaaaXXXXXX
OCI_SUBNET_OCID=

# Instance Shape - ARM free tier shape
# Value: VM.Standard.A1.Flex
OCI_INSTANCE_SHAPE=VM.Standard.A1.Flex

# Shape Config - ARM free tier limits (4 cores, 24GB RAM)
OCI_INSTANCE_OCPUS=4
OCI_INSTANCE_MEMORY_GB=24

# Boot Volume Size - Free tier allows up to 200GB
OCI_BOOT_VOLUME_GB=200

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Create Oracle Cloud account: https://cloud.oracle.com/
# 2. Generate API key: openssl genrsa -out ~/.oci/oci_api_key.pem 2048
# 3. Generate public key: openssl rsa -pubout -in ~/.oci/oci_api_key.pem -out ~/.oci/oci_api_key_public.pem
# 4. Add public key to Oracle Console: User Settings → API Keys → Add API Key
# 5. Copy fingerprint from Oracle Console after adding key
# 6. Fill in values above and save as .env
# 7. Test with: oci iam user get --user-id $OCI_USER_OCID
