# Upstream vectors

The fixture set, coverage matrix and upstream pins live in [`fixtures/`](fixtures/README.md), which `scripts/sync-fixtures.sh` keeps in sync with the host corpus (`--delete`). Do not hand-edit copies here.
