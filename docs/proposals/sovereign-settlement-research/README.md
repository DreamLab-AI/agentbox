# Sovereign settlement research pack (input to PRD-024, not authority)

Produced 2026-09-21 by a Fable-coordinated mesh: four Sonnet researchers (R1 upstream sidestr
spec and code; R2 agentbox surfaces; R3 solid-pod-rs, VisionClaw host, forum, AoE; R4 the
canon register of every prior RGB/402/ledger assertion), three Opus planners (integration
architecture, security and regulatory, DDD domain model) and the coordinator's fact base
with the owner's binding decisions D0 to D6. These are working inputs with `file:line`
citations verified at the time of writing; the decisions live in `docs/adr/ADR-2096` to
`ADR-2103`, the scope in `sovereign-settlement.md` (PRD-024) and the domain in
`sovereign-settlement-domain.md` (DDD-022). Where a plan and an ADR disagree, the ADR wins
(known divergence: PLAN-integration names the manifest block `[sidechain]` from revision 2;
the ADRs adopted that; PLAN-domain and earlier plan text may still say `[payments.sidestr]`).
The independent adversarial reviews (Sonnet red team, GPT-6 Astra via Codex) are filed
alongside when complete.
