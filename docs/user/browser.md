# This page has moved

> This file is kept only because existing links point at it. The content now
> lives at **[`linked-object-viewer.md`](linked-object-viewer.md)**.

The JSON-LD linked-data viewer (`[linked_data.viewer]`, PRD-006 §15/S12) — a
read-only web UI for agentbox's own emitted documents — is documented at
[`docs/user/linked-object-viewer.md`](linked-object-viewer.md).

Looking for browser *automation* (navigating pages, screenshots, WebGPU
testing) instead? That's the external browsercontainer sidecar — see the
`browser` / `browser-automation` skills and
[configuration.md](configuration.md#skills) `[skills.browser]`.
