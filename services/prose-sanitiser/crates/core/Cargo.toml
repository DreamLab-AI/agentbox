[package]
name = "prose-sanitiser-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared types for the prose-sanitiser workspace: findings, spans, patches, severity and confidence tiers, and the check/fix trait shapes. No I/O, no subprocesses."
keywords = ["prose", "linter", "unicode", "sanitiser", "provenance"]
categories = ["text-processing", "command-line-utilities"]
readme = "README.md"

[dependencies]
serde_json = { workspace = true }
