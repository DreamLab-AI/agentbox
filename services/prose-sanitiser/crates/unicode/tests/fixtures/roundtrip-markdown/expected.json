{
  "id": "roundtrip-markdown",
  "group": "round-trip",
  "tool": "clean-file",
  "input": "input.md",
  "args": [],
  "description": "Markdown with front matter, a fenced code block and a link — all span types the sanitiser must exclude before any rule fires.",
  "expect": {
    "kind": "byte-identical"
  }
}
