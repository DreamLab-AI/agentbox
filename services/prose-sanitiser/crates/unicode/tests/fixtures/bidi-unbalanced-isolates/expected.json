{
  "id": "bidi-unbalanced-isolates",
  "group": "adversarial",
  "tool": "clean-text",
  "input": "input.txt",
  "args": [],
  "description": "An isolate initiator with no terminator, so the directional state leaks into everything that follows it on the page.",
  "expect": {
    "kind": "must-strip",
    "codepoints": [
      8295
    ]
  }
}
