{
  "id": "trojan-source-python",
  "group": "adversarial",
  "tool": "clean-text",
  "input": "input.py",
  "args": [],
  "description": "Trojan Source in Python using isolates rather than overrides, so a check that only knows U+202E misses it.",
  "expect": {
    "kind": "must-strip",
    "codepoints": [
      8294,
      8297,
      8238
    ]
  }
}
