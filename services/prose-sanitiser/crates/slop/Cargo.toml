[package]
name = "prose-sanitiser-slop"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Deterministic AI writing-tell scanners: lexical, structural and narrative slop rules for prose and Markdown, plus design anti-pattern detection for source. Report-only, no model, no network."
keywords = ["slop", "ai-detection", "linter", "prose", "writing"]
categories = ["text-processing", "command-line-utilities"]
readme = "README.md"

[dependencies]
prose-sanitiser-core = { workspace = true }
prose-sanitiser-uk = { workspace = true }
regex = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
