[package]
name = "colloquy-view"
version = "0.1.1"
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
authors.workspace = true
license = "Apache-2.0"
description = "Presentation models for colloquy knowledge units: a unit as a readable thread, honest confidence display, and the tooling-gap board. Pure, wasm-ready, and depends only on colloquy-core."
readme = "README.md"
keywords = ["agents", "knowledge", "cq", "ui"]
categories = ["data-structures"]

[dependencies]
colloquy-core = { path = "../colloquy-core", version = "0.1" }
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
