[package]
name = "system-one-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
authors.workspace = true
license = "Apache-2.0"
description = "The System One typed-decision protocol in pure Rust: choice / score / noul request and response types, validation, token budgeting, option fitting, state windowing and per-window aggregation. No I/O, no async, wasm-capable."
readme = "README.md"
keywords = ["llm", "classification", "protocol", "typed-decisions"]
categories = ["api-bindings", "data-structures", "text-processing"]
documentation = "https://docs.rs/system-one-core"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
indexmap = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
