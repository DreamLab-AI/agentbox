# Podcast Knowledge Ingest — weekly Monday 06:17 UTC
# Managed by supervisord via supercronic. Edit this file to change schedule.
# Channel list: podcasts.yaml (same directory, or --config path)

# supercronic runs jobs via /bin/sh with only the supervisor PATH. All three
# jobs need more than that: coreutils for `tee`, node for submit-proposals.mjs,
# the python env for `yt-dlp` (which podcast-ingest still shells out to), and
# the podcast-ingest/podcast-promote binaries themselves.
#
# There is deliberately NO `PATH=` line here any more. It used to pin Nix store
# paths by hand — which went stale on every image rebuild, and silently
# overrode whatever the supervisor exported. The PATH is now built by Nix in
# flake.nix's [program:podcast-cron] environment= and inherited by these jobs,
# so store paths can never drift out of sync with the image again.

# LIVE with assertion-ledger integration (option 4) since 2026-08-23: curated
# pages are never edited; assertions land on podcast-evidence___* ledger pages
# with [[Topic]] wikilinks (sandbox e2e verified: 0 curated pages modified,
# pipeline.validate clean, idempotent). First runs capped in podcasts.yaml.
17 6 * * 1 /home/devuser/workspace/project/agentbox/skills/podcast-knowledge-ingest/run-ingest.sh /home/devuser/workspace/project/agentbox/skills/podcast-knowledge-ingest/podcasts.yaml 2>&1 | tee -a ${VAULT_TRANSCRIPTS}/.ingest-log.txt

# Promotion stage 2h after ingest (Monday 08:17 UTC): survivors -> proposal
# dossiers for the governed queue; rejects -> vault workingGraph news pages.
# Idempotent (fingerprint-banked), capped at 15/week; judge-unavailable
# candidates defer cleanly and retry next week.
17 8 * * 1 /home/devuser/workspace/project/agentbox/skills/podcast-knowledge-ingest/run-promote.sh 2>&1 | tee -a ${VAULT_TRANSCRIPTS}/.promote-log.txt

# Stage 3 (Monday 08:47): submit survivor dossiers to the governed ontology
# proposal queue (Whelk gate -> ACSP human approval). Idempotent per
# fingerprint set; unresolvable targets are skipped, never guessed.
47 8 * * 1 cd /home/devuser/workspace/project/agentbox/skills/podcast-knowledge-ingest && node submit-proposals.mjs 2>&1 | tee -a ${VAULT_TRANSCRIPTS}/.promote-log.txt
