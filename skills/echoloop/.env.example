# DEPRECATED — retired 2026-07-15, pointer kept 2026-07-22 (PRD-024 C-8, append-only culture).
#
# EchoLoop's env keys (ECHOLOOP_LLM_PROVIDER, ECHOLOOP_TRANSCRIBER,
# ECHOLOOP_WHISPER_MODEL/DEVICE, ECHOLOOP_SYSTEM_DEVICE, ECHOLOOP_MIC_DEVICE,
# ECHOLOOP_LOG_DIR, DEEPGRAM_API_KEY) are now in the single canonical template:
#   ../../.env.example   (repo root)
#
# See docs/reference/env-consolidation-plan.md and
# docs/reference/audit-2026-07-15.md.
#
# Do not add new keys here. Do not delete this file — it is kept as a
# navigation pointer for anyone who still has this path bookmarked.
