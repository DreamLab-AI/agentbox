# DEPRECATED — retired 2026-07-15, pointer kept 2026-07-22 (PRD-024 C-8, append-only culture).
#
# This was "the real skill-key surface" (61 keys, incl. CERAMIC_API_KEY). All
# unique keys were merged into the single canonical template:
#   ../.env.example   (repo root)
#
# See docs/reference/env-consolidation-plan.md for the full rationale
# (9 tracked env template files, 3 disagreeing key vocabularies -> one source
# of truth) and docs/reference/audit-2026-07-15.md for the originating audit.
#
# Do not add new keys here. Do not delete this file — it is kept as a
# navigation pointer for anyone who still has this path bookmarked.
