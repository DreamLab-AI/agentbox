# codex-registered-skills.txt — the curated set of skills exposed to the OpenAI Codex CLI
# (GPT-6 Astra). Reconciled at boot into ~/.codex/skills by scripts/reconcile-skills.sh
# (CLAUDE_SKILLS_DIR=$HOME/.codex/skills REGISTERED_SKILLS_MANIFEST=<this file>), symlinking
# each entry from /opt/agentbox/skills/<name>. Independent of registered-skills.txt (the
# Claude Code surface) on purpose: Codex caps its always-loaded skill index at 2 % of context
# or 8,000 characters and shortens/drops descriptions past that, so this list stays short and
# favours skills whose bodies do not depend on Claude-only affordances (Agent/fork, Artifact,
# Skill tool, /route, claude-flow hooks). Everything else stays reachable on demand through
# /opt/agentbox/skills/SKILL-DIRECTORY.md and skill-router/references/routing-table.md, which
# ~/.codex/AGENTS.md points at.

# Discovery and delivery
skill-router
codebase-memory
build-with-quality
docs-alignment
explainer
codebase-video

# Coding and repository workflows
rust-development
agentic-jujutsu
github-code-review
security-testing

# Browser, spatial, design, writing
browser
qgis
blender
open-design
prose-sanitiser

# Knowledge and shared memory
ontology-augment
ruvnet-brain
