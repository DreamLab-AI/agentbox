# explainer

Turn a codebase into a proven, dual-audience explainer bundle (three audience documents,
one visual page, one queryable knowledge base, all gated). Read `SKILL.md`. Not registered
with the `Skill` tool yet; invoke by reading `SKILL.md`. First instance:
`co-created/campaignbuilder/docs/explainer/` (Sep 2026).
