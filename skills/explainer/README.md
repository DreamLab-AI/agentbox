# explainer

Family hub for three codebase-explainer deliveries — a docs bundle, an
instructional microsite, or a video — sharing one grounding contract. Read
`SKILL.md`. Not registered with the `Skill` tool yet; invoke by reading
`SKILL.md`. The first two pilots (docs and microsite) were run against the
same private repo in September 2026; their evals live in `evals/`.
